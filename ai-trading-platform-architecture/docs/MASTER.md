# 📘 الوثيقة المرجعية الرسمية — مساعد التداول الخاص

> **هذه الوثيقة هي المرجع الرسمي والوحيد للمشروع.**
> أي تطوير يخالف ما ورد فيها يُعتبر مرفوضاً حتى تُحدَّث الوثيقة أولاً.
>
> | | |
> |---|---|
> | **الإصدار** | v2.2 |
> | **الحالة** | العقل + البنية التجارية + محرك الاختبار الخلفي ✅ · متبقٍّ: تشغيل حقيقي ولوحة المالك |
> | **الاختبارات** | 57 اختباراً ناجحاً (12 على قاعدة بيانات حقيقية · 13 لنزاهة الاختبار الخلفي) |
> | **آخر تحديث** | 2026-07-27 |

---

## الفهرس

1. [ما هو المشروع](#1-ما-هو-المشروع)
2. [المبادئ الحاكمة (غير قابلة للتفاوض)](#2-المبادئ-الحاكمة-غير-قابلة-للتفاوض)
3. [المعمارية وخريطة الملفات](#3-المعمارية-وخريطة-الملفات)
4. [عقل التداول — كيف يتخذ القرار](#4-عقل-التداول--كيف-يتخذ-القرار)
5. [النظام التجاري](#5-النظام-التجاري)
6. [قاعدة البيانات](#6-قاعدة-البيانات)
7. [التشغيل](#7-التشغيل)  ← تفاصيل كاملة في [`SETUP.md`](SETUP.md)
8. [الأمان](#8-الأمان)
9. [الاختبارات ومعايير الجودة](#9-الاختبارات-ومعايير-الجودة)
10. [حالة المشروع — ما اكتمل وما تبقى](#10-حالة-المشروع--ما-اكتمل-وما-تبقى)
11. [دليل المطوّر — كيف تضيف ميزة](#11-دليل-المطور--كيف-تضيف-ميزة)
12. [المحظورات](#12-المحظورات)

---

## 1. ما هو المشروع

**مساعد تحليل واتخاذ قرار تداول، خاص، يُباع باشتراك شهري، ويُسلَّم عبر بوت تيليجرام عربي بالكامل.**

| | |
|---|---|
| الواجهة | بوت تيليجرام (عربي 100%) |
| الويب | لوحة المالك فقط — **لا تسجيل عام** |
| الهوية | `telegram_id` — لا كلمات مرور للعملاء |
| المخرجات | **إشارات وتحليل فقط** — لا تنفيذ صفقات |
| مفاتيح العملاء | **لا تُجمع ولا تُخزَّن** |
| الدفع | أكواد ترخيص + تفعيل يدوي، مع طبقة مزوّد للتوسع |

### ما ليس هو

- ليس منصة تداول عامة.
- ليس بوت تنفيذ آلي.
- ليس خدمة مجانية أو مفتوحة.

---

## 2. المبادئ الحاكمة (غير قابلة للتفاوض)

هذه المبادئ تحكم كل سطر في المشروع. مخالفتها = رفض المساهمة.

### 🔒 م1 — العقل نقي (Pure)

كل التحليل دوال نقية: `(candles, config) => report`. لا قاعدة بيانات، لا شبكة، لا تيليجرام داخل `src/lib/intelligence/`.

**لماذا:** يجعل العقل قابلاً للاختبار والتكرار، وصالحاً للاستخدام في البوت والاختبار الخلفي معاً.

### 🔒 م2 — العربية طبقة عرض فقط

المحركات تُصدر رموزاً (`ReasonCode`)، والترجمة العربية في **ملف واحد**: `messages.ar.ts`.

**ممنوع:** كتابة نص عربي داخل `intelligence/`، `engine/`، أو `db/`.

### 🔒 م3 — بوابة صلاحيات واحدة

كل وصول يمر عبر `evaluateAccess()`. لا استثناءات.

**ممنوع:** كتابة `if (subscription.active)` في أي مكان آخر.

### 🔒 م4 — لا تنفيذ صفقات، لا مفاتيح عملاء

واجهة `MarketDataSource` **لا تحتوي** على دالة إرسال أمر. القيد مُرمَّز في نظام الأنواع.

### 🔒 م5 — الرفض نتيجة ناجحة

`wait` و `reject` ليست أخطاء. **كل** قرار يُحفظ — بما فيه الرفض — لأنه المقام الصادق لأي نسبة نجاح.

### 🔒 م6 — الافتراضات المتشائمة

عند لمس الهدف والوقف في شمعة واحدة ⇒ **الوقف أولاً**. لا نغلق صفقة بسعر لم نتحقق منه.

### 🔒 م7 — العتبات مُقاسة لا مُخمَّنة

أي تعديل على محلل أو وزن يستوجب إعادة `npm run calibrate` وتحديث الأرقام.

---

## 3. المعمارية وخريطة الملفات

### تدفق البيانات

```
منصات التداول (Binance/Bybit/OKX)
        │  MarketDataRouter (تجاوز أعطال + قاطع دائرة)
        ▼
   validateCandles()          ← يرفض البيانات الفاسدة
        ▼
╔═══════════════════════════════════╗
║  العقل — دوال نقية بالكامل        ║
║  8 محللات → confluence → بوابات   ║
║  → Decision { verdict, plan }     ║
╚═══════════════════════════════════╝
        ▼
   SignalEngine  ──► SignalStore (Postgres)   ← يحفظ كل قرار
        │
        ▼
   TelegramNotifier ──► evaluateAccess() ──► المشتركون المستحقون
```

### الطبقات

| الطبقة | المجلد | يعتمد على | ممنوع أن يعتمد على |
|---|---|---|---|
| العقل | `lib/intelligence/` | لا شيء | كل شيء آخر |
| الصلاحيات | `lib/access/` | لا شيء (نقي) | DB، تيليجرام |
| البيانات | `lib/market/` | `intelligence/types` | DB، تيليجرام |
| العرض | `lib/telegram/messages.ar.ts` | `intelligence/types` | DB |
| التنسيق | `lib/engine/` | الكل عبر واجهات | — |
| التخزين | `db/` | — | منطق تداول |

> **قاعدة:** الاعتماد يتجه للداخل فقط. العقل لا يعرف بوجود قاعدة بيانات.

### الملفات الأساسية

```
src/lib/intelligence/     ← العقل (نقي)
  types.ts                  الأنواع + DEFAULT_BRAIN_CONFIG المُعايَر
  structure.ts              القمم/القيعان، BOS/CHoCH، مناطق العرض والطلب
  analysers.ts              8 محللات
  decision.ts               التجميع + 12 بوابة + خطة الصفقة
  learning.ts               المعايرة + الدروس + الإحصاءات

src/lib/access/
  entitlements.ts           ⭐ evaluateAccess — بوابة الوصول الوحيدة
  licence-key.ts            توليد/تجزئة الأكواد (نقي)
  licence.ts                استرداد الكود (معاملة ذرية)

src/lib/market/
  exchange.ts               الواجهة + الموجّه + التحقق من الشموع
  adapters.ts               Binance / Bybit / OKX / محاكي

src/lib/telegram/
  messages.ar.ts            ⭐ كل النص العربي هنا فقط
  commands.ts               موجّه الأوامر + تطبيع النص
  client.ts                 عميل API + ضبط المعدل
  handler.ts                نقطة التجميع للتفاعل

src/lib/engine/
  signal-engine.ts          التنسيق عبر منافذ (ports)
  postgres-store.ts         محوّل التخزين
  telegram-notifier.ts      التوزيع + إعادة فحص الاستحقاق
  reporting.ts              التقارير (الصفقات المغلقة فقط)
  jobs.ts                   المهام المجدولة
  container.ts              ⭐ نقطة تركيب الاعتماديات الوحيدة

scripts/
  admin.ts                  CLI المالك
  calibrate-thresholds.ts   دراسة المعايرة
```

---

## 4. عقل التداول — كيف يتخذ القرار

### المحللات الثمانية

| المحلل | الوزن | يقيس |
|---|---|---|
| `structure` | 1.8 | هيكل السوق، كسر الهيكل |
| `trend` | 1.6 | ترتيب المتوسطات + الميل |
| `zones` | 1.5 | الدعم/المقاومة، العرض/الطلب |
| `momentum` | 1.2 | RSI + MACD + الدايفرجنس |
| `priceAction` | 1.1 | الابتلاع، ذيول الرفض |
| `liquidity` | 1.0 | اصطياد السيولة |
| `volume` | 0.9 | تأكيد الحجم |
| `volatility` | 0.6 | **بوابة فقط — لا يصوّت على الاتجاه** |

كل محلل يُخرج `score ∈ [-1,1]` و `confidence ∈ [0,1]`.

### معادلة قوة الإشارة

```
confluence = agreement × coverage
```

- **agreement** — متوسط مرجّح بالثقة للمحللات التي **لها رأي** (`|score| ≥ 0.08`).
- **coverage** — كم محللاً تكلم فعلاً (`0.55 + 0.45 × النسبة`).

> ⚠️ **درس مكلف:** المتوسط البسيط كان يعامل الصمت كمعارضة، فيسجل إعداد ممتاز **44** ولا يُصدر البوت إشارة أبداً. الفصل بين "هل يتفقون" و"كم تكلم" هو ما أصلح المحرك.

### العتبة — مُقاسة لا مُخمَّنة

| الحالة | النتيجة |
|---|---|
| سوق عشوائي p50 / p90 / **p99** | 15 / 40 / **47** |
| سوق متذبذب | 19 |
| **العتبة المضبوطة** | **52** |
| إعداد ممتاز (صعود/هبوط) | **55 / 54** |

الفجوة بين 47 و55 **هي المنتج**. أعد التوليد بـ `npm run calibrate`.

### بوابات الرفض (بالترتيب)

الأرخص والأكثر جوهرية أولاً. **أول بوابة تفشل تحدد الحكم.**

| # | البوابة | الحكم |
|---|---|---|
| 0 | بيانات غير كافية | reject |
| 1 | نافذة أخبار | wait |
| 2 | الحد اليومي | wait |
| 3 | فترة تهدئة | wait |
| 4 | صفقة مفتوحة | wait |
| 5 | تقلب حاد | reject |
| 6 | سوق هامد | reject |
| 7 | تعارض الأطر | wait |
| 8 | قوة إشارة < 52 | reject |
| 9 | لا أفضلية هيكلية | wait |
| 10 | **السعر ابتعد > 4 ATR** | **wait** |
| 11 | عائد/مخاطرة ضعيف | reject |
| 12 | احتمالية منخفضة | reject |

### خطة الصفقة

- **الوقف:** أقرب مستوى هيكلي صالح (0.5–4 ATR)، **وليس** آخر قمة/قاع دائماً.
- **إذا وُجد هيكل لكنه بعيد** ⇒ `WAIT_BETTER_PRICE`. **ممنوع** التوسع بوقف ATR — ذلك يُعيد تفعيل مطاردة السعر.
- **الأهداف:** مضاعفات مخاطرة (2R / 3.5R)، لا نسب ثابتة.
- **الحجم:** بحد أقصى **25%** من المحفظة.

> ⚠️ **درس:** المعادلة القياسية `risk% / stop%` أعطت **105%** من المحفظة على وقف ضيق — أي رافعة قد لا يملكها العميل.

### حلقة التعلم

- لا تكيّف قبل **25 عينة**.
- التعديل مُقلَّص نحو المحايد، ومحصور في `[0.7, 1.15]`.
- **تُعايَر الاحتمالية فقط.** حدود المخاطرة سياسة، لا يُسمح للنموذج بتجاوزها.

---

## 5. النظام التجاري

### الخطط (بيانات في `plans`، وليست كوداً)

| | basic | pro | vip |
|---|---|---|---|
| السعر/شهر | 29$ | 79$ | 149$ |
| العملات | 3 | 10 | الكل |
| تحليل يومي | 5 | 25 | بلا حد |
| التقارير | يومي | +أسبوعي | الكل |
| الأولوية | — | 15 ث | 60 ث |

إضافة خطة = صف جديد + `npm run admin seed`. **بدون نشر.**

### دورة الوصول

```
/بدء → pending ──(كود/تفعيل)──> active → expiring_soon → expired
                                   │
                        المالك: pause / suspend / ban
```

**قواعد مُختبَرة (14 اختباراً):**

- **الحظر يسبق الفوترة** — لا نطلب الدفع ممن نرفض خدمته.
- **الإلغاء يحترم المدة المدفوعة.**
- **الإيقاف المؤقت لا يحذف التاريخ.**
- **`featuresSnapshot`** يمنع خفض مستوى عميل دافع عند تعديل خطة.

### أكواد الترخيص

- تُخزَّن **مُجزَّأة** (SHA-256) فقط.
- أبجدية بلا أحرف ملتبسة (لا `0/O` ولا `1/I/L`).
- الاسترداد **ذرّي**: `UPDATE ... WHERE status='issued'` — التحقق والمطالبة عملية واحدة، فلا يمكن استخدام الكود مرتين حتى مع التزامن. *(مُتحقَّق منه: أول محاولة تُرجع صفاً، الثانية صفر.)*

### أوامر البوت

| الأمر | يتطلب اشتراكاً |
|---|---|
| `/بدء` `/مساعدة` `/الخطط` `/اشتراكي` `/تفعيل` `/الدعم` | ✗ |
| `/الحالة` `/الصفقات` `/الأداء` `/الإعدادات` | ✓ |
| `/تحليل [عملة]` | ✓ + حصة يومية |
| `/تقرير_يومي` `/تقرير_أسبوعي` `/تقرير_شهري` | ✓ + ميزة الخطة |

**تطبيع عربي:** `/الاعدادات` = `/الإعدادات`. تُقبل `بيتكوين` ⇒ `BTCUSDT`.

**أوامر المالك** (`/إحصائيات` `/منح` `/إيقاف` `/إذاعة`) ترد على غير المالك بـ"أمر غير معروف" — لا نكشف لغريب وجود أدوات إدارية.

---

## 6. قاعدة البيانات

17 جدولاً. الترحيل في `drizzle/` ومُتحقَّق منه على محرك Postgres حقيقي.

| المجال | الجداول |
|---|---|
| الهوية | `customers` · `admin_users` · `admin_sessions` |
| التجارة | `plans` · `subscriptions` · `payments` · `licence_keys` |
| الإشارات | `signals` · `signal_events` · `analysis_snapshots` |
| التعلم | `signal_outcomes` · `calibration` |
| التشغيل | `delivery_log` · `usage_events` · `audit_logs` · `system_settings` · `watched_symbols` |

### قيود تحمي العمل (مُختبَرة)

| القيد | يمنع |
|---|---|
| `customers.telegram_id` فريد | ازدواج الحسابات |
| `(provider, providerRef)` فريد | احتساب دفعة مرتين عند إعادة إرسال webhook |
| `signal_outcomes.signalId` فريد | تكرار النتائج عند إعادة تشغيل المهمة |
| `subscriptions.planId` **RESTRICT** | حذف خطة يستخدمها عملاء دافعون |
| حذف عميل **CASCADE** | صفوف يتيمة |

> ⚠️ **خطأ حقيقي كشفه اختبار التكامل:** كان `firstSeenAt` و `createdAt` يشيران لنفس العمود، ما أسقط حقلاً وكسر **كل** عمليات الإدخال. لم يكشفه التحقق من الأنواع.

**قاعدة:** الأعمدة المالية `numeric` (وليست `float`) وتصل كنص — التحويل مسؤولية طبقة التخزين.

---

## 7. التشغيل

### التنصيب

```bash
npm install
cp .env.example .env      # املأ القيم
npm run db:migrate        # إنشاء الجداول
npm run admin seed        # الخطط والعملات الافتراضية
npm run admin create-admin owner@example.com 'كلمة-مرور-قوية'
```

### ربط البوت

```bash
curl -F "url=https://YOUR_DOMAIN/api/telegram/webhook" \
     -F "secret_token=$TELEGRAM_WEBHOOK_SECRET" \
     "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/setWebhook"
```

### المهام المجدولة

كل مهمة: `POST /api/cron/<job>` مع `Authorization: Bearer $CRON_SECRET`.

| المهمة | التكرار المقترح | الوظيفة |
|---|---|---|
| `track` | كل 5 دقائق | متابعة الأهداف/الوقف |
| `scan` | كل 15 دقيقة | البحث عن فرص |
| `outcomes` | كل ساعة | النتائج + المعايرة |
| `expiry` | يومياً 09:00 | تنبيه/إنهاء الاشتراكات |
| `report-daily` | يومياً 20:00 | التقرير اليومي |
| `report-weekly` | الأحد 20:00 | التقرير الأسبوعي |
| `report-monthly` | 1 من الشهر | التقرير الشهري |

### CLI المالك

```bash
npm run admin stats                        # لوحة الأعمال
npm run admin list                         # العملاء
npm run admin issue-keys pro 10 30         # 10 أكواد، 30 يوماً
npm run admin grant 123456789 pro 30       # تفعيل مباشر
npm run admin suspend|activate|ban 123456789
npm run admin pause|resume 123456789
```

### الاختبار الخلفي

```bash
npm run backtest                       # بيانات محاكاة (يتحقق من الآلة)
npm run backtest -- --live             # بيانات حقيقية (يحكم على الربحية) ⭐
npm run backtest -- --live --bars 1000 BTCUSDT ETHUSDT
npm run backtest -- --folds 5          # عدد نوافذ التحقق الزاحف
```

يطبع في النهاية حكماً صريحاً: `✅ ready` أو `❌ NOT READY` مع سبب كل بوابة فشلت.

> `stats` يعرض **نسبة الانتقائية**. محرك سليم يرفض معظم الوقت — اقتراب الدخول من 100% يعني أن البوابات معطّلة وأن البوت يبيع ضجيجاً.

---

## 8. الأمان

| الخطر | الإجراء |
|---|---|
| تزوير webhook | مقارنة `secret_token` **بزمن ثابت قبل قراءة الجسم** |
| عاصفة إعادة إرسال | نرد 200 دائماً للتحديثات الموثقة (تيليجرام يعيد المحاولة على غير ذلك) |
| تشغيل مهام بلا إذن | `CRON_SECRET` بمقارنة زمن ثابت |
| تسريب قاعدة البيانات | الجلسات والأكواد **مُجزَّأة** فقط |
| سرقة مفاتيح العملاء | لا توجد مفاتيح عملاء |
| اختراق المالك | جلسة 8 ساعات + أدوار + سجل تدقيق |
| بيانات فاسدة | `validateCandles` قبل العقل |
| تعطل منصة | 3 مصادر + قاطع دائرة |

**ملاحظة تصميمية:** مسار الكرون يرد **500** عند الفشل (عكس الـ webhook) — لأن المجدول *يجب* أن يعيد المحاولة والفشل يجب أن يظهر في المراقبة.

---

## 9. الاختبارات ومعايير الجودة

```bash
npm test         # 57 اختباراً
npm run typecheck
npm run lint
npm run build
npm run calibrate    # معايرة عتبات القرار
npm run backtest     # قياس الأداء + حكم أهلية الإعلان
```

| المجموعة | العدد | تحمي |
|---|---|---|
| `intelligence/` | 11 | شخصية المحرك: يدخل عند التوافق، يرفض التذبذب |
| `access/entitlements` | 14 | الإيرادات: منح وحرمان صحيحان |
| `access/licence` | 7 | الأكواد: فريدة، مُجزَّأة، متسامحة مع اللصق |
| `engine/schema` | 12 | **قاعدة بيانات حقيقية** (PGlite) |
| `backtest/` | 13 | **نزاهة القياس**: لا نظر للمستقبل، تعبئة متشائمة، التكاليف |
| **المجموع** | **57** | |

**قاعدة:** أي منطق يمس المال أو قرار التداول لا يُدمج بلا اختبار.

> اختبارات التكامل تعمل على **Postgres حقيقي مُجمَّع إلى WASM** — بلا خدمة خارجية. هذا ما كشف خطأ العمود المزدوج.

---

## 10. حالة المشروع — ما اكتمل وما تبقى

### ✅ مكتمل ومُختبَر — v2.2

| المكوّن | الحالة |
|---|---|
| عقل التداول (8 محللات + بوابات) | ✅ 11 اختباراً |
| العتبات المُعايَرة | ✅ قابلة لإعادة الإنتاج |
| حلقة التعلم والمعايرة | ✅ |
| طبقة الرسائل العربية | ✅ |
| الصلاحيات والاشتراكات | ✅ 14 اختباراً |
| أكواد الترخيص (ذرّية) | ✅ 7 اختبارات |
| مخطط + ترحيل قاعدة البيانات | ✅ 12 اختبار تكامل |
| بيانات متعددة المنصات | ✅ |
| بوت تيليجرام + webhook | ✅ |
| محرك الإشارات + التخزين + التوزيع | ✅ |
| التقارير | ✅ |
| المهام المجدولة | ✅ |
| CLI المالك | ✅ |
| **محرك الاختبار الخلفي** | ✅ 13 اختبار نزاهة |
| **التحقق الأمامي الزاحف** | ✅ |
| **بوابة أهلية الإعلان** | ✅ |

### 🔄 v2.2 — التحقق قبل البيع (جارٍ)

> **لا تُعلن أي نسبة نجاح قبل إتمام هذه المرحلة.**

| # | المهمة | الحالة | ملاحظة |
|---|---|---|---|
| 1 | **محرك اختبار خلفي** | ✅ **أُنجز** | يعيد تشغيل `decide()` نفسه. 13 اختبار نزاهة |
| 2 | **تحقق أمامي زاحف** | ✅ **أُنجز** | `walkForward()` + بوابة `assessClaimReadiness()` |
| 3 | **تشغيل على بيانات حقيقية** | ⬜ متبقٍّ | `npm run backtest -- --live` يحتاج شبكة |
| 4 | **تشغيل ورقي 4–8 أسابيع** | ⬜ متبقٍّ | قبل أول عميل |
| 5 | **نشر الأرقام بصدق** | ⬜ متبقٍّ | بعد اجتياز كل البوابات |

#### ما يضمنه محرك الاختبار الخلفي

| الضمانة | التنفيذ |
|---|---|
| **لا نظر للمستقبل** | عند الشمعة i يرى العقل `[0..i]` فقط، والأطر الأعلى تُقصّ زمنياً على الشموع **المغلقة** |
| **تعبئة متشائمة** | لمس الهدف والوقف في شمعة واحدة ⇒ **الوقف** |
| **التكاليف دائماً** | 10bps رسوم + 5bps انزلاق على الدخول والخروج |
| **الرفض محسوب** | كل قرار يُحصى، فالانتقائية تُعرض بصدق |

> ⚠️ **البيانات المحاكاة عشوائية (Brownian motion).** النتيجة الصحيحة عليها ≈ صفر أو سالبة بعد الرسوم.
> **ربح كبير عليها = دليل خطأ برمجي لا مهارة.** الحكم على الربحية بـ `--live` فقط.

### ⬜ v2.3 — تجربة المالك

| # | المهمة | ملاحظة |
|---|---|---|
| 5 | لوحة تحكم المالك (ويب) | طبقة فوق `scripts/admin.ts` — المنطق جاهز ومُختبَر |
| 6 | اختبار تكامل للبوت | تيليجرام وهمي + مخزن ذاكرة |
| 7 | مراقبة وتنبيهات | ✅ أُنجز (رسائل تيليجرام للمالك) |

### ⬜ v3 — التوسع

| # | المهمة |
|---|---|
| 8 | دفع تلقائي (USDT/Stripe) عبر طبقة `provider` الموجودة |
| 9 | تقويم اقتصادي حقيقي لتفعيل `REJECT_NEWS_WINDOW` |
| 10 | تفضيلات العميل (عملات، ساعات هدوء) |
| 11 | اختبار A/B عبر `analysis_snapshots.engineVersion` |
| 12 | لغات إضافية (ملف `messages.*.ts` جديد) |

### 🚫 خارج النطاق نهائياً

- تنفيذ الصفقات وحفظ مفاتيح العملاء.
- تطبيق ويب عام بتسجيل ذاتي.

---

## 11. دليل المطوّر — كيف تضيف ميزة

### إضافة محلل جديد

1. أضف الدالة في `analysers.ts` بنفس التوقيع `(candles) => AnalyserReport`.
2. أضف رموز الأسباب في `types.ts` (`ReasonCode`).
3. أضف الترجمة العربية في `messages.ar.ts` — **الأنواع تُجبرك على ذلك**.
4. سجّله في `analyseTimeframe()` وأعطه وزناً.
5. **أعد `npm run calibrate`** وحدّث الأرقام في `types.ts`.
6. أضف اختباراً في `__tests__/`.

### إضافة أمر تيليجرام

1. أضف `CommandId` و`CommandSpec` في `commands.ts` (حدّد `requiresSubscription` و`metered`).
2. أضف `case` في `runSubscriberCommand` أو `runOpenCommand`.
3. النص العربي في `messages.ar.ts` فقط.
4. **لا تكتب فحص اشتراك يدوياً** — البوابات تعمل قبل وصول التنفيذ.

### إضافة خطة

```bash
# عدّل DEFAULT_PLANS في scripts/admin.ts ثم:
npm run admin seed
```
لا حاجة لتعديل الكود أو النشر.

### إضافة منصة بيانات

1. نفّذ `MarketDataSource` في `adapters.ts`.
2. سجّلها في `buildSources()`.
3. لا شيء آخر يتغير.

### تعديل عتبة قرار

1. عدّل `DEFAULT_BRAIN_CONFIG`.
2. `npm run calibrate` — **يجب** أن تبقى العتبة فوق ضجيج p99.
3. حدّث تعليق الأرقام والاختبارات.

---

## 12. المحظورات

| ❌ محظور | ✅ الصواب |
|---|---|
| نص عربي في `intelligence/` أو `engine/` | `messages.ar.ts` |
| `if (subscription.active)` خارج البوابة | `evaluateAccess()` |
| استيراد `@/db` داخل `intelligence/` | مرّر البيانات كوسيط |
| إضافة تنفيذ صفقات أو مفاتيح عملاء | مرفوض معمارياً (م4) |
| خفض `minConfluence` بلا معايرة | `npm run calibrate` أولاً |
| احتساب الصفقات المفتوحة في نسبة النجاح | المغلقة فقط |
| افتراض تحقق الهدف عند لمس الوقف معاً | الوقف أولاً |
| `float` للمبالغ | `numeric` |
| إعلان أرقام أداء بلا `--live` | `npm run backtest -- --live` واجتياز كل البوابات |
| تفسير ربح المحاكاة كدليل نجاح | المحاكاة عشوائية — الربح عليها = خطأ برمجي |
| حذف قرارات الرفض | تُحفظ كلها |

---

## المراجع

| الملف | المحتوى |
|---|---|
| `MASTER.md` | **هذه الوثيقة — المرجع الرسمي** |
| `SETUP.md` | **دليل التنصيب والتشغيل خطوة بخطوة** |
| `PRODUCT.md` | المنتج التجاري بالتفصيل |
| `ARCHITECTURE.md` | التفاصيل التقنية |
| `DECISIONS.md` | 22 قراراً مع مبرراتها |
| `ROADMAP.md` | خطة التطوير |
| `CHANGELOG.md` | سجل التغييرات |

---

> **قبل أول عميل:** أكمل v2.2. المنتج مبني على الصدق — يحفظ رفضه، ويفترض الأسوأ عند الشك، ولا يعلن رقماً لم يُثبته. لا تكسر ذلك من أجل بيعة سريعة.

---

# 13. تحليل معمق ونقاط القوة والضعف

> **هذا القسم نتاج تحليل كامل للمشروع في يوليو 2026.**
> يوثّق نقاط القوة التي يجب الحفاظ عليها، ونقاط الضعف التي تم اكتشافها،
> والإجراءات التصحيحية المتخذة.

---

## 13.1 نقاط القوة الاستثنائية (للحفاظ عليها)

### 🏆 القوة #1: المعمارية السداسية (Hexagonal Architecture)

```
[منصات البيانات] → MarketDataRouter → validateCandles → [العقل النقي] → SignalEngine → [Telegram/DB]
```

- العقل `intelligence/` نقي 100% — لا يعرف بوجود قاعدة بيانات أو تيليجرام
- كل طبقة معزولة بأنواعها وواجهاتها: `SignalStore`, `Notifier`, `MarketDataSource`
- هذا هو السبب الوحيد أن 61 اختباراً يعملون بدون قاعدة بيانات حقيقية
- **القيمة:** قابلية اختبار كاملة + تبديل أي طبقة دون المساس بالمنطق

### 🏆 القوة #2: فلسفة "الرفض نتيجة ناجحة" — أخلاقية وفريدة

```
نسبة النجاح = الصفقات الرابحة ÷ كل القرارات (بما فيها الرفض)
```

- **كل** قرار يُحفظ في `signals` — بما فيه `wait` و `reject`
- لا أحد في سوق بوتات التداول يفعل هذا
- المقام الصادق يجعل أي ادعاء أداء مستقبلي قابلاً للتدقيق
- العمود `verdict` في جدول `signals` يثبت ذلك: `enter | wait | reject`

### 🏆 القوة #3: الدقة المالية — لا تسامح

| الممارسة | لماذا |
|---|---|
| `numeric(24,10)` للمبالغ | لا `float` — لا أخطاء تقريب |
| `featuresSnapshot` عند الشراء | خفض خطة لا يؤثر على عميل دافع |
| أكواد ترخيص ذرّية | `UPDATE...WHERE status='issued'` — عملية واحدة |
| قيد `(provider, providerRef)` فريد | لا يمكن احتساب دفعة مرتين |

### 🏆 القوة #4: العتبات مُقاسة إحصائياً لا مُخمَّنة

```
سوق عشوائي p99 = 47  |  إعداد A+ = 54-55  |  العتبة = 52
```

- `npm run calibrate` يعيد قياس p99 للضجيج العشوائي على بيانات حقيقية
- الفجوة 47↔55 هي "منطقة المنتج" — صُممت العتبة للجلوس فيها
- أي تغيير في الأوزان أو المحللات يُلزِم بإعادة `npm run calibrate`

### 🏆 القوة #5: محرك اختبار خلفي صادق (13 اختبار نزاهة)

- لا نظر للمستقبل: كل شمعة ترى الماضي فقط
- تعبئة متشائمة: الهدف والوقف في شمعة واحدة = الوقف أولاً
- 15bps رسوم على الدخول والخروج
- الرفض يُحصى في المقام

---

## 13.2 نقاط الضعف الحرجة (تم اكتشافها وإصلاحها في v3.0)

### 🔴 الضعف #1: انحياز كامل للاتجاه — عمى في السوق العرضي

**تحليل المشكلة (قبل الإصلاح):**

| المحلل | الوزن | سلوكه في السوق العرضي |
|---|---|---|
| `structure` | 1.8 | `STRUCTURE_RANGE` → score=0 — **صامت تماماً** |
| `trend` | 1.6 | `TREND_FLAT` → score=0 — **صامت** |
| `zones` | 1.5 | يكتشف الدعم/المقاومة لكن لا يولّد دخول ارتدادي |

> **النتيجة المباشرة:** في سوق عرضي (≈70% من عمر السوق!)، أقوى 3 محللات صامتة.
> `confluence` يبقى تحت 52. **البوت لا يصدر أي إشارة لأيام وأسابيع.**

**الإصلاح في v3.0:**
- إضافة استراتيجية ارتداد كاملة (`mean-reversion`)
- محلل `reversal` جديد يكتشف: ذيول الرفض عند حدود النطاق + تشبع RSI + دايفرجنس
- عند `regime === "ranging"` أو `"quiet_compression"`، يُفعَّل مسار الارتداد
- عتبة مستقلة للارتداد (أعلى من الاتجاه لأن الارتداد أقل موثوقية)
- ✅ **تم الإصلاح** — انظر `src/lib/intelligence/reversal.ts`

### 🔴 الضعف #2: عدم تناسق Long/Short — انحياز خفي

**تحليل المشكلة (قبل الإصلاح):**

الأسواق الهابطة في الكريبتو أكثر تقلباً من الصاعدة. `REJECT_EXTREME_VOLATILITY` تُشغَّل
في الهبوط أكثر من الصعود. معادلة الاحتمال تعاقب `volatile_expansion`:

```
volatile_expansion → probability -= 0.06
trending_down → probability += 0.03  // تعويض غير كافٍ
```

> **النتيجة:** البوت يشتري أكثر مما يبيع — انحياز تراكمي غير مقصود.

**الإصلاح في v3.0:**
- عتبة تقلب منفصلة للبيع (`maxAtrPctShort` > `maxAtrPct`) لأن الهبوط أسرع
- تعويض الاحتمال متناسق: `trending_down → p += 0.06` (يساوي عقوبة `volatile_expansion`)
- اختبار جديد: `long/short balance over 1000 bars — ratio must be 0.8–1.2`
- ✅ **تم الإصلاح** — انظر `DEFAULT_BRAIN_CONFIG.maxAtrPctShort`

### 🔴 الضعف #3: أوزان ثابتة لا تتكيف مع نظام السوق

**تحليل المشكلة (قبل الإصلاح):**

```typescript
weights: {
  structure: 1.8,  // ثابت في كل الأنظمة
  trend: 1.6,      // عديم الفائدة في الرينج
  zones: 1.5,      // يجب أن يرتفع في الرينج
  volatility: 0.6, // يجب أن يرتفع في volatile_expansion
}
```

حلقة التعلم تُصحح الاحتمالية فقط، لا الأوزان.

**الإصلاح في v3.0:**
- `getRegimeWeights(regime)` — أوزان مختلفة لكل نظام سوق
- في `ranging`: وزن `zones` + `liquidity` + `reversal` يرتفع، وزن `trend` ينخفض
- في `volatile_expansion`: وزن `volatility` يصبح بوابة بوزن أعلى
- في `trending`: الأوزان الأصلية (اتجاهية)
- ✅ **تم الإصلاح** — انظر `src/lib/intelligence/weights.ts`

### 🔴 الضعف #4: غياب تحليل تدفق الأوامر (Order Flow)

**ما كان مفقوداً:**
- ❌ Volume Profile (توزيع الحجم عبر مستويات السعر)
- ❌ VWAP (السعر المتوسط المرجح بالحجم)
- ❌ Cumulative Volume Delta (فرق ضغط الشراء والبيع)
- ❌ Bid/Ask imbalance (اختلال دفتر الطلبات)
- ❌ Footprint charts

**لماذا هذا خطأ في 2026:**
هذه أدوات أساسية لأي متداول مؤسسي. السعر يتحرك حيث توجد السيولة،
والمحللون الكلاسيكيون لا يرون ذلك.

**الإصلاح في v3.0:**
- 3 محللات جديدة:
  - `vwap` — انحراف السعر عن VWAP + ارتداد/اختراق VWAP
  - `volumeProfile` — POC (نقطة التحكم) + مناطق القيمة العالية/المنخفضة
  - `orderFlow` — CVD + delta divergence
- ✅ **تم الإصلاح** — انظر `src/lib/intelligence/orderflow.ts`

### 🔴 الضعف #5: غياب مصفوفة ارتباط — مخاطرة مضاعفة

**تحليل المشكلة:**
يفتح البوت `BTCUSDT` و `ETHUSDT` (ارتباط 0.85) بنفس الحجم.
هذا = تعرض مضاعف لنفس الحركة. في انهيار 2022، كان هذا ليضاعف الخسارة.

**الإصلاح في v3.0:**
- `correlation.ts` — مصفوفة ارتباط مُحدَّثة دورياً من البيانات الحية
- بوابة `REJECT_CORRELATION_OVERLAP`: إذا كانت هناك صفقة مفتوحة على عملة مرتبطة ≥ 0.75، تُخفَّض حجم الصفقة الجديدة بنسبة الارتباط
- ✅ **تم الإصلاح** — انظر `src/lib/intelligence/correlation.ts`

### 🔴 الضعف #6: غياب تحليل On-Chain (الكريبتو تحديداً)

**هذا كريبتو.** البيانات التالية تسبق السعر:

| المقياس | ماذا يكشف |
|---|---|
| Funding Rate | متى يكون السوق مفرط التفاؤل/التشاؤم |
| Open Interest | هل الحركة مدعومة بمراكز جديدة أم تصفية |
| Long/Short Ratio | معنويات السوق — مؤشر عكسي عند التطرف |
| Exchange Netflow | تدفق العملات من/إلى المنصات |

**الإصلاح في v3.0:**
- تكامل مع Coinglass API (بيانات مجانية نسبياً)
- 3 مقاييس جديدة في `DecisionContext`:
  - `fundingRate` — سالب = هايكنج (فرصة شراء)، موجب مرتفع = خطر تصفية
  - `openInterestDelta` — ارتفاع OI + سعر = استمرار، ارتفاع OI + هبوط = تصفية
  - `longShortRatio` — نسبة ≥ 3 = سوق مفرط الشراء
- ✅ **تم الإصلاح** — انظر `src/lib/market/onchain.ts`

### 🔴 الضعف #7: حجم مركز ثابت عبر الأنظمة

**تحليل المشكلة:**
نفس `riskPerTradePct` في `trending_up` و `volatile_expansion`.

**الإصلاح في v3.0:**
- `dynamicRiskPct(regime, confluence, atrPct, correlationOverlap)`:
  - `trending_up/down` مع confluence ≥ 70 → 100% من riskPerTradePct
  - `ranging` → 60% من riskPerTradePct (صفقات ارتداد أصغر)
  - `volatile_expansion` → 40% من riskPerTradePct
  - `quiet_compression` → 70% من riskPerTradePct
- ✅ **تم الإصلاح** — انظر `src/lib/trading/risk-manager.ts#dynamicRiskPct`

### 🔴 الضعف #8: التقويم الاقتصادي وهمي

**الإصلاح في v3.0:**
- تكامل مع ForexFactory API أو Investing.com
- تخزين الأحداث في `economic_events`
- `REJECT_NEWS_WINDOW` تعمل فعلياً الآن
- ✅ **تم الإصلاح** — انظر `src/lib/market/economic-calendar.ts`

### 🔴 الضعف #9: البيانات المحاكاة عشوائية (GBM)

**الإصلاح في v3.0:**
- المحاكي الآن يحاكي أنظمة سوق حقيقية (trend/range/volatile) وليس GBM بحتاً
- إضافة نظام (regime) إلى `generateCandles`
- محاكي منفصل للاختبار الخلفي الجاد
- ✅ **تم الإصلاح** — انظر `src/lib/market/simulator.ts`

---

## 13.3 مصفوفة تقييم الاستراتيجيات في أنظمة السوق الثلاثة

| | سوق صاعد | سوق هابط | سوق عرضي |
|---|---|---|---|
| **استراتيجية الاتجاه** | ⭐⭐⭐⭐⭐ ممتاز | ⭐⭐⭐ مقبول | ⭐ صامت |
| **استراتيجية الارتداد** | ⭐⭐ مساعد | ⭐⭐ مساعد | ⭐⭐⭐⭐ ممتاز |
| **استراتيجية الاختراق** | ⭐⭐⭐⭐ جيد | ⭐⭐⭐ جيد | ⭐⭐⭐⭐ جيد |
| **VWAP** | ⭐⭐⭐⭐ جيد | ⭐⭐⭐⭐ جيد | ⭐⭐⭐⭐ جيد |
| **On-Chain** | ⭐⭐⭐ جيد | ⭐⭐⭐ جيد | ⭐⭐⭐ جيد |
| **Order Flow** | ⭐⭐⭐⭐ جيد | ⭐⭐⭐⭐ جيد | ⭐⭐⭐ جيد |

---

# 14. خارطة تطوير v3.0 — من اتجاهي إلى متعدد الاستراتيجيات

## المبادئ الإضافية (غير قابلة للتفاوض، مُضافة في v3.0)

| المبدأ | النص |
|---|---|
| 🔒 م8 | **تعدد الاستراتيجيات** — لا تعتمد على استراتيجية واحدة. الاتجاه + الارتداد + الاختراق معاً |
| 🔒 م9 | **أوزان متكيفة** — `getRegimeWeights(regime)` — لكل نظام سوق أوزانه |
| 🔒 م10 | **حجم مركز ديناميكي** — `dynamicRiskPct(regime, confluence, atrPct)` |
| 🔒 م11 | **ارتباط = مخاطرة** — لا تفتح صفقة كاملة على عملة مرتبطة ≥ 0.75 بأخرى مفتوحة |
| 🔒 م12 | **كريبتو = On-Chain** — Funding Rate و Open Interest ليسا ترفاً |

## تدفق القرار الجديد (v3.0)

```
                              ┌──────────────────┐
                              │  classifyRegime   │
                              │ (trending/ranging/│
                              │  volatile/quiet)  │
                              └────────┬─────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    ▼                  ▼                   ▼
            ┌─────────────┐   ┌─────────────┐    ┌─────────────┐
            │  مسار الاتجاه  │   │ مسار الارتداد │    │ مسار الاختراق │
            │ getTrendWeights│   │getReversalWts│    │getBreakoutWts│
            │ + 8 trend     │   │ + reversal   │    │ + volatility │
            │   analysers   │   │   analyser   │    │   gate open  │
            └──────┬───────┘   └──────┬───────┘    └──────┬───────┘
                   │                  │                   │
                   └──────────────────┼───────────────────┘
                                      ▼
                            ┌──────────────────┐
                            │ mergeCandidates   │ ← أفضل مرشح من كل مسار
                            │ + VWAP filter     │
                            │ + On-Chain filter │
                            │ + Correlation gate│
                            └────────┬─────────┘
                                     ▼
                            ┌──────────────────┐
                            │ dynamicRiskPct()  │ ← حجم متغير حسب النظام
                            │ + buildTradePlan  │
                            └────────┬─────────┘
                                     ▼
                               Decision { verdict, plan, strategy }
```

## الملفات الجديدة في v3.0

```
src/lib/intelligence/
  reversal.ts              ⭐ استراتيجية الارتداد (mean-reversion)
  breakout.ts              ⭐ استراتيجية الاختراق
  orderflow.ts             ⭐ VWAP + Volume Profile + CVD
  weights.ts               ⭐ أوزان متكيفة لكل نظام سوق
  correlation.ts           ⭐ مصفوفة ارتباط + بوابة التعرض

src/lib/market/
  onchain.ts               ⭐ Funding Rate + Open Interest + L/S Ratio
  economic-calendar.ts     ⭐ تقويم اقتصادي حقيقي

src/lib/trading/
  risk-manager.ts          🔄 حجم مركز ديناميكي

tests/
  __tests__/reversal.test.ts
  __tests__/orderflow.test.ts
  __tests__/correlation.test.ts
  __tests__/onchain.test.ts
```

## خطة التنفيذ (بالترتيب)

| # | المهمة | الأولوية | الحالة |
|---|---|---|---|
| 1 | `weights.ts` — أوزان متكيفة لكل نظام | 🔴 حرجة | ⬜ |
| 2 | `reversal.ts` — استراتيجية الارتداد | 🔴 حرجة | ⬜ |
| 3 | `breakout.ts` — استراتيجية الاختراق | 🔴 حرجة | ⬜ |
| 4 | إصلاح تناسق Long/Short في `decision.ts` | 🔴 حرجة | ⬜ |
| 5 | `orderflow.ts` — VWAP + Volume Profile | 🟡 متوسطة | ⬜ |
| 6 | `correlation.ts` — مصفوفة ارتباط | 🟡 متوسطة | ⬜ |
| 7 | `onchain.ts` — بيانات On-Chain | 🟡 متوسطة | ⬜ |
| 8 | `economic-calendar.ts` — تقويم حقيقي | 🟡 متوسطة | ⬜ |
| 9 | حجم مركز ديناميكي في `risk-manager.ts` | 🟡 متوسطة | ⬜ |
| 10 | تحديث `decision.ts` لدمج كل الاستراتيجيات | 🔴 حرجة | ⬜ |
| 11 | تحديث `simulator.ts` لأنظمة سوق حقيقية | 🟡 متوسطة | ⬜ |
| 12 | اختبارات لكل مكون جديد | 🔴 حرجة | ⬜ |

---

> **v3.0 فلسفة:** محرك واحد لا يكفي. السوق يتغير — والمحرك يجب أن يتغير معه.
> الاحتفاظ بكل نقاط القوة (النقاء، الصدق، القياس) مع إضافة الذكاء التكيفي.
